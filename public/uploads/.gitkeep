# Uploaded Files Directory

This directory stores user-uploaded files (product images and videos).

## Security Notes

- Only admin users can upload files
- File uploads are validated by type and size (max 10MB)
- Files are stored with random filenames to prevent directory traversal
- This directory is served as static files via the public folder
